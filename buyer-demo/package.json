{
  "name": "patron-buyer-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The demo's customer: a standalone AI agent with its own funded Circle Agent Wallet that discovers Patron, hits the x402-gated /api/hire endpoint, gets 402 Payment Required, signs an EIP-3009 authorization via Circle MPC, and pays — no human in the loop.",
  "scripts": {
    "wallet:setup": "tsx src/wallet-setup.ts",
    "deposit": "tsx src/deposit.ts",
    "demo": "tsx src/run.ts"
  },
  "dependencies": {
    "@circle-fin/developer-controlled-wallets": "^10.8.0",
    "@circle-fin/x402-batching": "^3.2.0",
    "dotenv": "^16.4.5",
    "viem": "^2.55.4"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
